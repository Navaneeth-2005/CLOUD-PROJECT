type Notification @model @auth(rules: [{ allow: owner, ownerField: "userId" }]) {
  userId: ID!
  notificationId: ID!
  type: String!
  entityId: ID!
  message: String!
  isRead: Boolean!
  createdAt: AWSDateTime!
}

# Query to list notifications for a user (auto‑generated by @model)
# Subscription for real‑time updates when a notification is created
